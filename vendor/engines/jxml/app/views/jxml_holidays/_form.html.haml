= simple_form_for(@jxml_holiday) do |f|
  = f.input :date, as: :string, input_html: { class: "datepicker", value: format_usa_date(f.object.date) }
  = f.button :submit, t(".submit"), class: ["btn", "btn-primary"]
  = link_to t(".cancel"), jxml_holidays_path

  :javascript
    $(function () { $(".datepicker").datepicker(); });
